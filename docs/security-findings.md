# fz-mart Security Findings

| ID | Area | Severity | File:Line | Description | Status |
| --- | --- | --- | --- | --- | --- |
| A1-01 | Server action authz | Info | src/app/(storefront)/funnel-actions.ts:10 | `recordAddToCart` / `recordCheckoutStart` are unauthenticated and take a caller-supplied `productId` with no validation. Impact is analytics pollution only — no read, write or disclosure of business data. Accepted as public by design. | Open (accept?) |
| A1-02 | Server action authz | Info | src/app/(storefront)/products/recently-viewed-actions.ts:19 | `resolveRecentlyViewed` is unauthenticated; resolves caller-supplied slugs to public catalog data already visible on the storefront. No private data reachable. | Open (accept?) |
| A1-03 | Server action authz | Info | src/i18n/actions.ts:8 | `setLocale` is unauthenticated but validates via `isLocale()` allowlist before writing the locale cookie. No injection path. | Open (accept?) |
| A1-04 | Server action authz | Low | src/app/(storefront)/newsletter-actions.ts:18 | `subscribeNewsletter` is unauthenticated with no rate limit, unlike sibling public actions which all call `rateLimit`. Allows unbounded subscriber-list stuffing / mail-send amplification. Inconsistent with established pattern. | Open |
| A1-05 | Server action authz | Info | src/app/(storefront)/order-confirmation/[orderNo]/self-service-actions.ts:25 | `cancelOrderAction` / `requestReturnAction` are unauthenticated by design; ownership proven by (orderNo, phone) via `findOwnedOrder` + `ipGuard()` rate limit. See A1-07 for the ownership-check weakness. | Open |
| A1-06 | Server action authz | Info | src/app/(storefront)/track/actions.ts:18 | `lookupOrder` unauthenticated by design; (orderNo, phone) pair + per-IP rate limit (20/5min). Documented as intentional. | Closed (by design) |
| A1-07 | Broken access control | Medium | src/server/orders/self-service.ts:16-24 | `findOwnedOrder` fetches by `orderNo` alone via `findUnique`, then compares `order.customerPhone !== phone` in JS after the fetch. Violates the project rule that ownership filters belong in the Prisma `where`. Also a non-constant-time string compare on the sole ownership secret. Feeds cancel + return actions. | Open |
| A1-08 | Server action authz | Info | src/app/(storefront)/products/[slug]/wishlist-actions.ts:35 | `notifyMeAction` intentionally public (back-in-stock for logged-out users); rate-limited per IP, customerId optional. | Closed (by design) |
| A1-09 | Server action authz | Info | src/app/(admin)/admin/login/actions.ts, forgot-password, reset-password, (storefront)/login | Auth entrypoints (`login`, `verifyTwoFactorLogin`, `logout`, `requestPasswordReset`, `resetPassword`, `requestMagicLink`) are necessarily unauthenticated — these establish the session. Not defects. | Closed (by design) |
| A1-10 | Server action authz | Info | src/app/(storefront)/checkout/actions.ts:58,84,130,146 | `requestCheckoutOtp`, `confirmCheckoutOtp`, `applyCoupon`, `placeOrder` are unauthenticated to support guest checkout. Business-logic risk (total tampering, coupon races) is out of scope for A1 — deferred to the later targeted sections. | Open (deferred) |
| A2-01 | IDOR | **High** | src/app/(storefront)/order-confirmation/[orderNo]/page.tsx:16 → src/server/orders/getOrder.ts:18 | `getOrderByOrderNo` fetches by `orderNo` alone with NO ownership check, and the page is public (no session, no rate limit). Changing the URL to another `FZnnnnnn` renders that customer's **name, phone number, line items and order total**. `orderNo` is 6 random digits (1e6 keyspace, `src/server/orders/orderNo.ts:9`) — enumerable in hours at a few hundred req/s against an unthrottled GET. Contrast `/track`, which requires phone AND is rate-limited: this page is the unthrottled bypass of that control. | **Fixed** — `getOrderForViewer` (src/server/orders/getOrder.ts) replaces the unauthenticated `getOrderByOrderNo` on the page. A viewer must either own the order (customerId INSIDE the where clause) or hold the short-lived grant issued at checkout (src/lib/order-access.ts). Unauthorised → `notFound()`, so the page is no longer an existence oracle. |
| A2-02 | IDOR → privilege | **High** | src/app/(storefront)/order-confirmation/[orderNo]/page.tsx:61-64 | Same page passes the victim's `order.customerPhone` into `<CustomerOrderActions phone=...>`. The phone is the sole ownership secret for `cancelOrderAction`/`requestReturnAction`, so the leak in A2-01 hands the attacker the credential needed to **cancel a stranger's order** or file a fraudulent return. Escalates A2-01 from disclosure to unauthorized state change. Depends on A1-07. | **Fixed** — the `phone` parameter is GONE from `cancelOrderAction`/`requestReturnAction` and from the `CustomerOrderActions` props. The actions now require the same grant and read the phone server-side off the authorised order, so the client can no longer name whose order it acts on. |
| A2-03 | IDOR | Low | src/app/(storefront)/payment/return/page.tsx:18 | `prisma.order.findUnique({ where: { orderNo } })` from an unauthenticated query param. Leaks no fields directly, but redirects a guessed `orderNo` into the vulnerable confirmation page above. Fix follows from A2-01. | **Fixed** — the success redirect is gated on `hasOrderAccess(orderNo)`, so a guessed orderNo no longer bounces into the confirmation page. |
| A2-04 | IDOR | Info | src/server/customers/addresses.ts:121,141,160 | Saved addresses are correct: every mutation gates on `findFirst({ where: { id, customerId } })` before update/delete. No finding — recorded as the reference pattern for fixing A2-01. | Closed (correct) |
| A2-05 | IDOR | Info | src/server/orders/getOrder.ts:26 | `trackOrder` correctly uses `findFirst({ where: { orderNo, customerPhone } })` — ownership inside the `where`. No finding. | Closed (correct) |
| A2-06 | IDOR | Info | src/server/orders/admin.ts:198,288,426; src/server/products/reviews-admin.ts:91,113; src/server/purchasing/index.ts | Admin-side `findUnique` by ID with no ownership filter. App is single-tenant (no Store/Tenant model, no storeId/tenantId in schema) and all callers sit behind `requirePermission`, so there is no cross-tenant boundary to violate. Not defects; would become defects if multi-store is ever added. | Closed (by design) |
| A2-07 | IDOR | Info | src/server/customer-cart/index.ts:33 | Cart keyed on session-derived `customerId`, never a client-supplied id. No finding. | Closed (correct) |
| A2-08 | Access control | Info | src/app/api/admin/upload/route.ts:32 | R2/upload route checks `getCurrentAdmin()` before any file handling and validates `folder` against the `STORAGE_FOLDERS` allowlist. No unsigned/user-controlled object paths. No finding. | Closed (correct) |
| A3-01 | Order tracking | Info | src/server/orders/getOrder.ts:26; src/app/(storefront)/track/actions.ts:18 | `/track` itself is built correctly: `findFirst({ where: { orderNo, customerPhone } })` (ownership in the `where`), both fields required, per-IP rate limit 20/5min. The endpoint is NOT the leak — A2-01 is. Recorded as the baseline the confirmation page fails to meet. | Closed (correct) |
| A3-02 | Order tracking | Medium | src/lib/rate-limit.ts:38-44 | The limiter **fails open**: any Redis error returns `{allowed:true}`. Deliberate and documented, and defensible for abuse-prevention — but here it is the only brake on a brute-forceable PII endpoint. If Redis is down/unprovisioned, `/track` becomes unlimited. Consider failing closed for `track:ip` specifically while keeping fail-open elsewhere. | Open |
| A3-03 | Order tracking | Medium | src/lib/client-ip.ts:7-11 | Rate limit is keyed on the first value of a client-supplied `X-Forwarded-For` header, with no trusted-proxy handling. An attacker rotating XFF gets a fresh 20-request budget per forged value, defeating the `/track` limit. `getClientIp` returns null with no header and callers then skip limiting entirely. | Open |
| A3-04 | Order tracking | Low | src/app/(storefront)/checkout/actions.ts:32 | The second factor is a BD mobile number matching `^01[3-9]\d{8}$` — a structurally constrained keyspace, not a random secret, and often semi-public. Combined with the 1e6 `orderNo` space this is a weak pair; it is adequate ONLY while the rate limit holds (see A3-02, A3-03). | Open |
| A3-05 | PII exposure | Info | src/app/(storefront)/track/TrackForm.tsx | Tracking response renders: customerName, customerPhone, full address, shippingZone, line items, subtotal/deliveryCharge/total, status, createdAt. A successful guess yields a complete delivery-address + phone record — i.e. the unit of data a competitor would scrape. Justifies treating A2-01 as High. | **Mitigated** — /track still renders this data, but it requires orderNo AND phone and is rate limited. The unauthenticated path that made it enumerable (A2-01) is closed. |
| B1-01 | Total tampering | Info | src/server/orders/createOrder.ts:14-19; src/app/(storefront)/checkout/actions.ts:146-206 | **No total-tampering vector found.** `CheckoutItemInput` is `{productId, variantId, quantity}` only — the interface the fix prompt asks us to create already exists. `placeOrder` reads no price/subtotal/total/discount/charge field from `formData`; the only numerics crossing the boundary are IDs and quantities. | Closed (correct) |
| B1-02 | Total tampering | Info | src/server/orders/createOrder.ts:134-203 | Unit price is re-resolved per line from the DB inside the checkout transaction (variant `discountPrice ?? price`, else product-level), then `subtotal += unitPrice * quantity`. Name + price are snapshotted onto OrderItem so historical orders can't be rewritten by later catalog edits. | Closed (correct) |
| B1-03 | Total tampering | Info | src/server/orders/createOrder.ts:73-85, 223 | `deliveryCharge` comes from `resolveDeliveryLocation(divisionId, districtId, upazilaId)` resolved against the DB **inside** the transaction, not from the client. Client supplies location IDs only. | Closed (correct) |
| B1-04 | Coupon integrity | Info | src/server/orders/createOrder.ts:282-296 | Coupon discount is recomputed by `redeemCoupon` inside the same transaction against server-built `couponLines`; the client-supplied `couponCode` string is re-validated, never a client-supplied discount amount. Total updated as `baseTotal - discount`. | Closed (correct) |
| B1-05 | Money type safety | Info | src/lib/money.ts; src/app/(admin)/admin/(protected)/orders/actions.ts:120-124 | Paisa/taka boundary is handled correctly on the admin financials path: form renders taka (`toTaka`), action converts with `takaToPaisa` (`Math.round`) before persisting. `updateOrderFinancials`'s own `Math.round` is a redundant integer guard, not a unit bug. No float reaches a money column. | Closed (correct) |
| B1-06 | Money rounding | Low | src/server/coupons/index.ts:112 | Percent coupons compute `Math.floor((eligible * coupon.value) / 100)` on integer paisa — floor rounds in the shop's favour, deterministic, no float. Not a vulnerability; noted only as the single rounding decision in the money path. | Closed (accepted) |
| B1-07 | Order integrity | Low | src/server/orders/createOrder.ts:251-253, 295 | Totals are computed correctly but never **asserted** before persist: there is no `sum(lines) + shipping - discount === total` invariant check, so a future refactor could silently write an inconsistent total. Defence-in-depth gap, not an exploitable flaw today. This is the one part of the B1 fix prompt still worth implementing. | Open |
